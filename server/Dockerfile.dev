# Development Dockerfile for Node.js backend with hot reloading
FROM node:18-alpine

# Install PostgreSQL client for health checks and migrations
RUN apk add --no-cache postgresql-client

WORKDIR /usr/src/app

# Copy package files
COPY package*.json ./

# Install dependencies
RUN npm ci

# Install nodemon for hot reloading
RUN npm install -g nodemon

# Expose port
EXPOSE 3000

# Start the application directly (initialization will be handled by docker-compose)
CMD ["npm", "run", "server:dev"]
